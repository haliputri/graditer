{
    "version": 2,
    "builds": [
      {
        "src": "!(static|templates)/**/*.py",
        "use": "@vercel/python",
        "config": { "zeroConfig": true }
      }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/.vercel/build"
      }
    ]
  }