{
    "version": 2,
    "routes": [
      { "src": "/static/(.*)", "dest": "/static/(.*)" },
      { "src": "/templates/(.*)", "dest": "/templates/(.*)" },
      { "src": "/templates/admin/(.*)", "dest": "/templates/admin/(.*)" },
      { "src": "/templates/components/(.*)", "dest": "/templates/components/(.*)" },
      { "src": "/(.*)", "dest": "/templates/login.html" },
      { "src": "/(.*)", "dest": "/aes.py" }
    ]
  }